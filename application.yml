
spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Samarth6840/config-repo
          default-label: main

server:
  port: 8000

eureka:
  client:
    service-url: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
